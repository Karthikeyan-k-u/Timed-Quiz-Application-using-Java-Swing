# Timed-Quiz-Application-using-Java-Swing
A Java Swing-based quiz application that displays multiple-choice questions with a 5-second timer. Users select answers before time runs out, and the app automatically moves to the next question. It calculates and displays the final score at the end.
